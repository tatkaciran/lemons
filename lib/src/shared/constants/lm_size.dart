class LMSize {
  /// 1360
  static double totalWidth = 1024; // 1360

  /// one quarter of [ totalWidth ]
  static double smallWidth = totalWidth / 4;

  /// one half of [ totalWidth ]
  static double mediumWidth = totalWidth / 2;

  /// 3/4 of [ totalWidth ]
  static double largeWidth = smallWidth * 3;
}
